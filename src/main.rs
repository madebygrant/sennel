mod config;
mod theme;

/* Wave 0.3 owns the event loop and the TUI shell. Until then this is only a
   harness so the ported modules compile and their tests run. */
fn main() {
    println!("sennel: tui shell lands in wave 0.3");
}
